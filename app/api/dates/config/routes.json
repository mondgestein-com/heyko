{
  "routes": [
    {
      "method": "GET",
      "path": "/dates",
      "handler": "dates.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/dates/count",
      "handler": "dates.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/dates/:id",
      "handler": "dates.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/dates",
      "handler": "dates.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/dates/:id",
      "handler": "dates.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/dates/:id",
      "handler": "dates.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
